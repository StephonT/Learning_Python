import math
#You can import math to have access to more math functions. This is a package built in with Python.

print(math.ceil(10.3))
print(math.floor(5.3))

# Round to the nearest whole number with the round() method

x = 2.9
print(round(x))

# ABS which is short for Absolute. It always return a positive number
b = -2.9
print(abs(b))